Verifică starea proiectului FoliiBuilder:

1. Rulează `dotnet build src/FoliiBuilder.Web` și verifică că nu sunt erori
2. Pornește serverul dacă nu e pornit (`dotnet run --project src/FoliiBuilder.Web &`)
3. Rulează `npm test` și raportează rezultatele
4. Rezumă: ce a trecut, ce a picat, ce trebuie reparat
